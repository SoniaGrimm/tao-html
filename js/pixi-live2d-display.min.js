<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Live2D Voiceflow Agent</title>
  <style>
    body {
      margin: 0;
      background: #111;
      overflow: hidden;
    }
    canvas {
      display: block;
    }
    #voiceflow-agent {
      position: fixed;
      bottom: 10px;
      right: 10px;
      width: 400px;
      height: 600px;
      border: none;
    }
  </style>
</head>
<body>
  <div id="app"></div>
  <iframe
    id="voiceflow-agent"
    src="https://general.voiceflow.com/your-project-id-here/embed" <!-- Remplace par ton lien Voiceflow -->
    allow="microphone; autoplay"
  ></iframe>

  <script src="js/pixi.min.js"></script>
  <script src="js/index.min.js"></script>
  <script>
    const app = new PIXI.Application({ width: window.innerWidth, height: window.innerHeight, transparent: true });
    document.body.appendChild(app.view);

    PIXI.live2d.Live2DModel.from('https://unpkg.com/live2d-widget-model-hijiki@1.0.5/assets/hijiki.model.json').then(model => {
      model.scale.set(0.3);
      model.x = app.renderer.width - 300;
      model.y = app.renderer.height - 500;
      app.stage.addChild(model);
    });
  </script>
</body>
</html>// pixi-live2d-display placeholder